import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stfs.comoros',
  appName: 'STFS Comoros',
  webDir: 'dist',
  server: {
    cleartext: true
  }
};

export default config;
